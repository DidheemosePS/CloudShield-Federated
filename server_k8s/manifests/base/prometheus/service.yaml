apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
spec:
  type: ClusterIP
  selector:
    app: prometheus-server
  ports:
    - name: prometheus-server
      port: 4000
      targetPort: 4000
